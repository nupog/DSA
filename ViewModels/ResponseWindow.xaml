<Window x:Class="DeepSeekSurveyAnalyzer.Views.ResponseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ответ DeepSeek" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Размышления:" FontWeight="Bold"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBox Text="{Binding Reasoning}" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas"/>
        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="Ответ:" FontWeight="Bold" Margin="0,10,0,0"/>
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <TextBox Text="{Binding Answer}" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas"/>
        </ScrollViewer>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="80" Margin="5"/>
            <Button Content="Сохранить ответ" Command="{Binding SaveAnswerCommand}" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>